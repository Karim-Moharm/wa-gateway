node_modules/

# Real secret (API_KEY) - .env.example is the template, this is not
.env

# WhatsApp login sessions - equivalent to account credentials, never share
.wwebjs_auth/
.wwebjs_cache/

# Keys minted by POST /provision - real per-client secrets, per-server
provisioned-keys.json
provisioned-keys.json.tmp

# OS/editor cruft
.DS_Store
*.log
